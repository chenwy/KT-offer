<div class="col-md-12">
  <h2><%= @job.title %></h2>

  <div class="text-center">
    <%= link_to(t("sendRecord"), new_job_resume_path(@job), style: "font-size: 30px; text-decoration: underline;") %>
  </div>
</div>
